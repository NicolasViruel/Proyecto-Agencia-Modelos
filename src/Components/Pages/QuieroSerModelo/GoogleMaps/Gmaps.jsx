import React from 'react';
import { useMemo } from 'react';
import {GoogleMap, useLoadScript, Marker} from "google-map-react"

const Gmaps = () => {
    const {} = useLoadScript({googleMapsApiKey: ""})
    return (
        <div>
            
        </div>
    );
};

export default Gmaps;